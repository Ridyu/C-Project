#include<stdio.h>
#include<time.h>

int main_arryproject(void) {


	/*int sum = 1;
	int i = 5;

	sum += 1;
	printf("%d\n", sum);

	sum += i;
		printf("%d", sum);*/

	//srand(time(NULL));
	//printf("\n\n ===아빠는 대머리 게임=== \n\n");

	//int answer; //사용자 입력값
	//int treatment = rand() % 4;	//발모제 선택 (4개) 0~3

	//int cntShowBottle = 0;		//이번 게임에 보여줄 병 갯수
	//int prevShowBottle = 0;		//앞 게임에 보여준 병 갯수
	////서로 보여주는 병 갯수를 다르게 하여 정률 향상 (처음 2개, 다음엔 3개)
	//// 
	////3번의 기회(3번의 발모제 투여 시도)
	//for (int i = 1; i <= 3; i++) {
	//	int bottle[4] = { 0,0,0,0 };	//총 4개의 병
	//	do {
	//		cntShowBottle = rand() % 2 + 2;		//보여줄 병 갯수 (0-1). +2 -> 2,3
	//	} while (cntShowBottle == prevShowBottle);
	//	prevShowBottle = cntShowBottle;

	//	int isincluded = 0;		//보여줄 병 중에 발모제가 포함되었는지 여부 (1 : 포함), (0 : 불포함)
	//	printf(">>   %d 번째 시도 : ", i);

	//	//보여줄 병 종류를 선택
	//	for (int j = 0; j < cntShowBottle; j++) {
	//		int randBottle = rand() % 4;	// 0-3

	//		//아직 선택되지 않은 병이면, 선택 처리(1로 바꿔주기)
	//		if (bottle[randBottle] == 0) {
	//			bottle[randBottle] = 1;
	//			if (randBottle == treatment) {
	//				isincluded = 1;
	//			}
	//		}
	//		else {
	//			j--;
	//		}
	//	}

	//	//사용자에게 문제 표시
	//	for (int k = 1; k <= 4; k++) {
	//		if (bottle[k] == 1) {
	//			printf("%d", k);
	//		}
	//	}
	//	printf("물약을 버리에 바릅니다.\n\n");

	//	if (isincluded == 1) {
	//		printf("성공, 머리가 자랐어요!!\n");
	//	}
	//	else {
	//		printf("실패, 머리가 안 자랐어요..ㅠㅠ\n");
	//	}

	//	printf("\b   .....계속 하려면 아무키나 누르세요...");
	//	getchar();		//아무키 누르면 다시 진행
	//}

	//printf("발모제는 몇 번일까요? >>>> ");
	//scanf_s("%d", &answer);

	//if (answer == treatment+1) {			//treatment+1  << rand() % 4는 0~3까지라서 +1로 값을 증가시켜서 정답을 찾아내기
	//	printf("\n\t >>> 정답입니다.\n");
	//}
	//else {
	//	printf("\n\t >>> 틀렸습니다.\n, >정답은 %d 입니다.", treatment + 1);
	//}

	//return 0;
}
